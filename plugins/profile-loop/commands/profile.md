---
description: Show the current learned profile (the text the model reads)
---

Call `pl_show_profile` and show the user their current profile exactly as the
model would read it. If they want to change something by hand, that is fine:
the profile is just text, and editing it directly is always allowed.
