---
description: Show what the loop has learned, what it is watching, and what is ready to apply
---

Call `pl_status` and present the result clearly.

Explain the three buckets in plain terms: what has crossed the threshold and is
ready to apply, what the loop is watching but holding on (and why, for example
mixed signal or only one session), and how many interactions have been seen. If
anything is ready, offer to run `/profile-loop:review` to see the proposed edits.
